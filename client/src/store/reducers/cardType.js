import {REGISTER} from '../actions/constants';

export const cardTypeReducer = (state = {}, {type, payload}) => {
    switch (type) {
        // case REGISTER:
        //     return payload;
        //     break;
        default:
            return state;
    }
}
